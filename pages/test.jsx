import { Container } from 'react-bootstrap'
import {data} from '../pages/api/teamData'

console.log()


export default function Test() {
  return (
    <Container>
      {/* {
        data.map((members) => {
          members.map(({image, name, title, description, link}, i) => (
            
          ))
        })
      } */}
    </Container>
  )
}